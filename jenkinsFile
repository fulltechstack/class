pipeline {
    agent any
    stages {
        stage('Install') {
            steps { 
            sh 'npm install' 
            echo "Installing..."
            }
        }
        stage('build') {
            steps { 
            sh 'npm run ng -- build' 
            echo "Building..."
            }
        }
        stage('deploy') {
            steps { 
            sh 'npx http-server dist/class -p 4200'
            echo "Deploying..."
         }
        }
       
    }
}
